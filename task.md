# 方向二：Workshop 研究项目
1. 什么是 Workshop

这里的 Workshop 指学术会议中的专题研讨会，通常围绕一个具体研究方向组织交流，并接收篇幅较短的论文。与大型学术会议主会论文相比，Workshop 更强调研究问题是否清楚、实验设计是否合理，以及实验结果能否支持研究结论。

Workshop 项目可以使用规模较小的数据集和模型，重点是完成一次完整的研究流程。本次招新不要求论文一定被某个具体 Workshop 接收，而是希望答题者按照 Workshop short paper 的标准完成研究，并为之后继续修改和尝试投稿打下基础。
2. 题目选择

Workshop 方向提供以下六个题目，进入该方向后再从中选择一个：

    题目一：预算受限条件下的中文 RAG 优化
    题目二：提高 LLM Agent 工具调用的可靠性
    题目三：标签噪声下的小样本图像分类
    题目四：参数高效微调在领域迁移中的表现
    题目五：缺失数据对时间序列异常检测的影响
    题目六：OCR 错误对文档问答的影响

如果希望选择其他研究问题，需要提前与 ML 招新负责人（qq：898534959）沟通并获得许可。自拟题目也应满足本节的研究和提交要求。
3. 题目一：预算受限条件下的中文 RAG 优化

研究在固定 token 数量、检索次数或推理时间的限制下，如何提高中文 RAG 系统的检索和回答效果。

基本要求：

    构建或整理一个规模适当的中文问答数据集；
    实现 BM25、向量检索或混合检索等 baseline；
    设计一种预算感知的检索、查询改写或重排序方法；
    对比不同方法的检索召回率和回答效果；
    分析检索错误、证据不足和模型幻觉等失败案例。

建议指标包括 Recall@k、Exact Match、F1、人工评价正确率、无依据回答比例、token 消耗和推理时间。
4. 题目二：提高 LLM Agent 工具调用的可靠性

构建一个包含若干模拟工具的 Agent 环境，研究工具选择错误、参数错误、重复调用和结果验证失败等问题，并尝试提高任务完成率。

基本要求：

    构建可控的工具调用任务环境；
    实现 direct prompting、ReAct 或其他合理的 baseline；
    设计一种规划、参数校验、结果验证或失败重试机制；
    设置不同难度和不同错误类型的任务；
    统计并分析 Agent 的失败原因。

建议指标包括 Task Success Rate、参数错误率、平均工具调用次数、平均响应时间和单任务 token 消耗。
5. 题目三：标签噪声下的小样本图像分类

在公开图像分类数据集中人为加入不同程度的错误标签，研究标签噪声对模型训练的影响，并设计方法提高模型在干净测试集上的表现。

基本要求：

    选择一个规模适当的公开图像分类数据集；
    构造 10%、20%、40% 等不同程度的标签噪声；
    实现普通交叉熵训练作为 baseline；
    设计一种样本筛选、样本重加权或鲁棒损失方法；
    完成不同噪声比例下的对比实验和消融实验；
    分析模型误判和噪声样本识别失败的原因。

建议指标包括 Accuracy、Macro-F1、Expected Calibration Error、噪声样本识别准确率，以及不同噪声比例下的性能变化。
6. 题目四：参数高效微调在领域迁移中的表现

选择一个文本分类任务，研究当训练数据和测试数据来自不同领域时，LoRA、Adapter 等参数高效微调方法的泛化表现，以及模型效果与训练成本之间的关系。

基本要求：

    选择标签定义一致、包含不同领域数据的文本分类数据集，明确训练域和测试域；
    使用同一预训练模型，对比全量微调、LoRA 和至少一种其他轻量方法，例如冻结编码器后仅训练分类头；
    同时评估模型在训练域和未参与训练的目标域上的表现，避免使用目标域测试集调参；
    选择 LoRA rank、学习率或训练数据量等因素，分析其对跨领域泛化的影响；
    基于实验发现提出并验证一种改进思路，完成必要的消融实验；
    分析不同领域上的典型错误和方法的适用条件。

建议指标包括 Accuracy、Macro-F1、可训练参数量、峰值显存占用、训练时间，以及跨领域性能下降幅度。
7. 题目五：缺失数据对时间序列异常检测的影响

在公开时间序列数据集中构造不同模式的缺失值，研究缺失数据对异常检测的影响，并探索将缺失位置作为额外信息输入模型，能否比单纯插值获得更好的检测效果。

基本要求：

    选择一个带有异常标注的公开时间序列数据集，合理划分训练集、验证集和测试集，避免时间窗口重叠造成数据泄漏；
    构造随机缺失、连续缺失等模式，并设置不同缺失比例；
    实现先插值再检测的 baseline，对比加入缺失掩码的方法；
    设计一种利用缺失信息的改进方法，例如加入距上次有效观测的时间间隔，或调整缺失位置的损失权重；
    在相同缺失设置下完成对比和消融实验，并使用验证集确定检测阈值；
    分析漏报、误报和检测延迟，说明方法在哪些缺失条件下有效或失效。

建议指标包括 AUROC、AUPRC、Precision、Recall、F1，以及不同缺失率下的性能变化；对于具有明确起止时间的异常事件，也可以评估检测延迟。
8. 题目六：OCR 错误对文档问答的影响

研究 OCR 识别中的错字、漏字和文本顺序错误如何影响文档问答系统，并尝试通过文本纠错、版面信息或置信度筛选降低这些错误带来的影响。

基本要求：

    选择一个规模适当、包含文档图像与问答标注的公开数据集，获取 OCR 文本并记录所使用的识别工具；
    在实际 OCR 结果之外，构造不同类型和强度的受控文本噪声，分别报告真实错误与模拟噪声下的实验结果；
    实现基于 OCR 文本检索或问答的 baseline；
    设计一种文本纠错、版面增强或置信度过滤方法；
    固定下游问答模型及其他主要设置，完成不同噪声强度下的对比实验和消融实验；
    分析不同 OCR 错误类型如何影响答案，并检查纠错方法是否引入了新的错误。

建议指标包括 Exact Match、Token-F1、字符错误率，以及不同噪声强度下的问答性能变化；如果包含检索步骤，也可以评估 Recall@k。字符错误率应在具有人工核对文本的样本上计算。
9. Workshop 方向提交材料

进入 Workshop 方向的答题者需要提交：

    一篇约 3–4 页的英文 short paper 或 letter；
    完整代码和运行说明；
    数据集说明、数据处理脚本和实验配置；
    实验结果、图表及必要的日志；
    AI 工具使用说明。

论文建议包含以下部分：

    Abstract
    Introduction
    Related Work
    Method
    Experiments
    Error Analysis
    Limitations and Conclusion